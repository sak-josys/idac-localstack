FROM amazon/aws-cli:latest

RUN yum install -y bash findutils >/dev/null && yum clean all

WORKDIR /workspace

# Override the base image's "aws" entrypoint so we run our shell script.
ENTRYPOINT ["/bin/bash", "/workspace/scripts/publish-airflow-to-localstack.sh"]
